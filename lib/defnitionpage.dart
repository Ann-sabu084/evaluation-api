import 'package:flutter/material.dart';

class Defnitionpage extends StatelessWidget {
  const Defnitionpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("defnition"),),);
  }
}